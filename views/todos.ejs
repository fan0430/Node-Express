<!DOCTYPE html>
<html lang="en">
<h1>Todos</h1>
<ul>

    <% for(let i=0; i < todos.length; i++) { %>
        <li>
            <%= todos[i]%>  <!--加上等於代表後面的東西要輸出-->
        </li>
        <% } %>
</ul>

</html>